/**
 * SearchPage — Phase 6 implementation target.
 *
 * Features (to build in Phase 6):
 * - Natural language query input box
 * - Search button with loading state animation
 * - POST to /api/search → forwards to FastAPI ML service
 * - Navigates to ResultPage with results
 */
export default function SearchPage() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>Search Videos</h1>
      <p style={{ color: "#888" }}>
        🚧 Phase 6 — Natural language query input + animated loading state. Coming soon.
      </p>
    </div>
  );
}
